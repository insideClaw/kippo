1. Orchestrate the `kubectl apply` deployment, moving the .yaml files to the appropriate directories
2. Optimize image size
3. Rework to a better supported honeypot software which handles modern SSH connections
